% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/max_diversity_clustering.R
\name{init_max_diversity_clustering}
\alias{init_max_diversity_clustering}
\title{Maximum diversity soft clustering}
\usage{
init_max_diversity_clustering(
  embedding,
  design_matrix,
  theta = 2,
  sigma = 0.1,
  nclust = min(round(ncol(embedding)/30), 100),
  tau = 0,
  block_size = 0.05,
  max_iter_cluster = 200,
  epsilon_cluster = 1e-05,
  verbose = TRUE
)
}
\arguments{
\item{embedding}{a matrix of size \verb{n_features x n_cells}}

\item{design_matrix}{a design matrix with \code{ncol(embedding)} rows that specifies
which cells should be diverse within a cluster (typically the batch or
patient variable).}

\item{theta}{diversity penalty. Larger values encourage more diverse clusters.
Can be a single value or one value per group in \code{design_matrix}. Default: \code{2}}

\item{sigma}{width of the soft-clustering kernel. Default: \code{0.1}}

\item{nclust}{number of clusters. Default: \code{min(round(ncol(embedding) / 30), 100)}}

\item{tau}{protection against overclustering small groups. Default: \code{0}}

\item{block_size}{fraction of cells to update per block in the online update
of the cluster assignments. Default: \code{0.05}}

\item{max_iter_cluster}{maximum number of iterations. Default: \code{200}}

\item{epsilon_cluster}{convergence threshold for the clustering iterations.
Default: \code{1e-5}}

\item{verbose}{Should progress be printed. Default: \code{TRUE}}
}
\value{
A list with the state of the clustering procedure (\code{Z_cos}, \code{Y}, \code{R},
\code{Phi}, \code{Pr_b}, \code{theta}, \code{sigma}, \code{E}, \code{O}, \code{K}, \code{N}, \code{B}, and bookkeeping
fields used by \code{\link{run_max_diversity_clustering}}).
}
\description{
A pure R re-implementation of the maximum diversity clustering step from
the Harmony batch-integration algorithm (Korsunsky et al. 2019, Nature Methods).
Iteratively fits soft cluster assignments that maximize the diversity
(in terms of a design/grouping variable, e.g. batch or patient) within each
cluster, using an entropy-regularized penalty. This is the piece of Harmony
that \code{\link{align_harmony}} uses; the rest of Harmony's algorithm (the ridge
regression based correction) is implemented natively by lemur (see
\code{align_impl()}).
}
\seealso{
\code{\link{run_max_diversity_clustering}}
}
\keyword{internal}
