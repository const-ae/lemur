% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/max_diversity_clustering.R
\name{run_max_diversity_clustering}
\alias{run_max_diversity_clustering}
\title{Run the maximum diversity clustering iterations}
\usage{
run_max_diversity_clustering(state, embedding = NULL)
}
\arguments{
\item{state}{the result of \code{\link{init_max_diversity_clustering}} or a previous
call to \code{run_max_diversity_clustering}.}

\item{embedding}{optionally, an updated embedding (e.g., after aligning the
cells) that replaces \code{state$Z_cos} before the clustering iterations start.
If \code{NULL} (default), the embedding of \code{state} is used unchanged.}
}
\value{
The updated \code{state} list (see \code{\link{init_max_diversity_clustering}}).
\code{state$objective_kmeans} contains the trace of the objective function for
this call (not accumulated across calls).
}
\description{
Alternates between updating the cluster centroids \code{Y} and the soft cluster
assignment \code{R} (with the diversity penalty) until convergence or
\code{state$max_iter_cluster} is reached.
}
\keyword{internal}
